# CLI Agent - OpenAI Chat

Интерактивное CLI-приложение для общения с LLM-моделями через OpenAI API.

## Возможности

- Интерактивный чат с LLM в командной строке
- Сохранение истории сообщений в формате JSON
- **Двухуровневое хранение сообщений с автоматической саммаризацией**
- Поддержка контекста диалога (history-aware)
- Обработка ошибок API (rate limit, network issues)
- Цветной вывод и индикаторы загрузки
- Автосохранение при выходе (SIGINT/SIGTERM)
- Логирование действий

## Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd day-09-summarization

# Установка зависимостей
npm install
```

## Настройка

1. Скопируйте `config.example.json` в `config.json`:

```bash
cp config.example.json config.json
```

2. Отредактируйте `config.json`, указав ваш API-ключ и параметры:

```json
{
  "apiBaseUrl": "https://openrouter.ai/api/v1",
  "model": "deepseek/deepseek-v3.2",
  "apiKey": "your-api-key-here",
  "temperature": 0.7,
  "recentMessagesCount": 3
}
```

### Параметры конфигурации

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `apiBaseUrl` | Базовый URL API | обязательно |
| `model` | Модель LLM | обязательно |
| `apiKey` | API-ключ | обязательно |
| `temperature` | Температура генерации | 0.7 |
| `recentMessagesCount` | Количество последних сообщений для хранения в оригинальном виде | 3 |

## Как работает саммаризация

Приложение использует **двухуровневую систему хранения сообщений**:

1. **Первый уровень** - Последние N сообщений (по умолчанию 3) хранятся в оригинальном виде
2. **Второй уровень** - Все сообщения старше N заменяются кратким саммари

### Процесс саммаризации:

1. При отправке сообщения LLM используется контекст: **саммари + последние N сообщений**
2. Когда накапливается 10 сообщений сверх N, запускается автоматическая генерация саммари
3. LLM генерирует краткое саммари, содержащее ключевые факты и темы
4. История обрезается до последних N сообщений
5. При каждом новом блоке из 10 сообщений саммари обновляется путем объединения с новым блоком

### Уведомления:

При генерации саммари вы увидите:
- 📝 "Генерация саммари предыдущих сообщений..." - перед началом
- ✅ "Саммари обновлено и сохранено" - после завершения
- История обрезана до последних N сообщений

## Запуск

```bash
npm start
```

## Использование

### Команды

- `/help` - Показать справку
- `/clear` - Очистить историю чата и саммари
- `/exit` / `/quit` - Выйти из программы

### Пример сессии

```
╔════════════════════════════════════════╗
║     CLI Agent - OpenAI Chat            ║
╚════════════════════════════════════════╝

Модель: deepseek/deepseek-v3.2
Введите ваше сообщение или команду

Доступные команды:
  /help    - Показать эту справку
  /clear   - Очистить историю чата
  /exit    - Выйти из программы
  /quit    - Выйти из программы

> Привет! Как дела?

🤖 Ответ:
Привет! Я готов помочь вам. Чем могу быть полезен?

...

📝 Генерация саммари предыдущих сообщений...
✅ Саммари обновлено и сохранено
История обрезана до последних 3 сообщений

> /exit
👋 До свидания!
```

## Структура проекта

```
├── config.json           # Конфигурация приложения
├── config.example.json  # Пример конфигурации
├── package.json         # Зависимости npm
├── tsconfig.json        # Настройки TypeScript
├── prompts.example.md   # Пример промтов для тестирования
├── .gitignore          # Исключения Git
├── .rooignore          # Исключения Roo
├── chat-history.json   # История сообщений (обрезается до N)
├── chat-summary.json   # Саммари сообщений
└── src/
    ├── index.ts        # Точка входа
    ├── types/
    │   └── index.ts   # TypeScript типы
    ├── config/
    │   └── index.ts   # Загрузка конфигурации
    ├── history/
    │   └── index.ts   # Работа с историей
    ├── summarizer/
    │   └── index.ts   # Модуль саммаризации
    ├── api/
    │   └── index.ts   # Интеграция OpenAI
    ├── cli/
    │   └── index.ts   # CLI интерфейс
    └── utils/
        └── logger.ts  # Логирование
```

### Назначение модулей

- **config** - Загрузка и валидация конфигурации
- **history** - Сохранение/загрузка истории чата в JSON
- **summarizer** - Автоматическая генерация и обновление саммари
- **api** - Взаимодействие с OpenAI API
- **cli** - Интерактивный интерфейс командной строки
- **utils** - Вспомогательные утилиты (логирование)

## NPM-скрипты

| Скрипт | Описание |
|--------|----------|
| `npm start` | Запуск приложения |
| `npm run dev` | Запуск в режиме разработки |
| `npm run build` | Компиляция TypeScript |

## Зависимости

### Основные

- `openai` - Официальный SDK OpenAI
- `chalk` - Цветной вывод в терминале

### Разработка

- `typescript` - Язык TypeScript
- `ts-node` - Запуск TypeScript
- `@types/node` - Типы Node.js

## Лицензия

ISC

## Contributing

1. Форкните репозиторий
2. Создайте ветку для изменений
3. Сделайте коммит с описанием
4. Отправьте Pull Request

## Notes

- При выходе (Ctrl+C или команда `/exit`) история и саммари автоматически сохраняются
- Логи записываются в файл `app.log`
- История чата хранится в `chat-history.json` (обрезается до N сообщений)
- Саммари хранится в `chat-summary.json`
- При команде `/clear` очищаются и история, и саммари
- Саммари используется как контекст для LLM вместо полной истории, что экономит токены
