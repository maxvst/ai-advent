# Сравнение LLM моделей

Программа на Node.js + TypeScript для сравнения качества ответов трёх LLM моделей разного уровня через OpenRouter API.

## Технологии

- **Node.js + TypeScript**
- **OpenAI SDK** — официальный клиент для работы с OpenRouter (OpenAI-совместимый API)
- **OpenRouter** — агрегатор LLM моделей с единым API

## Возможности

- 📊 Отправка одинакового запроса трём моделям разного уровня (сильная, средняя, слабая)
- ⏱️ Замер времени ответа, количества токенов (input/output), стоимости
- 🔒 Анонимное сравнение качества ответов (модели не знают авторов)
- 🏆 Итоговый вывод от сильной модели на основе всех данных
- 📄 Сохранение результатов в Markdown-отчёт

## Установка

```bash
npm install
```

## Настройка

1. Скопируйте `config.example.json` в `config.json`:
   ```bash
   cp config.example.json config.json
   ```
2. Откройте `config.json` и укажите ваш API ключ OpenRouter в поле `openRouter.apiKey`
3. При необходимости измените модели в секции `models`
4. Измените вопрос в поле `question`

### Пример конфигурации

```json
{
  "openRouter": {
    "baseUrl": "https://openrouter.ai/api/v1",
    "apiKey": "sk-or-..."
  },
  "models": {
    "strong": {
      "id": "anthropic/claude-3.5-sonnet",
      "name": "Claude 3.5 Sonnet",
      "pricing": { "input": 3.00, "output": 15.00 }
    },
    "medium": {
      "id": "anthropic/claude-3-haiku",
      "name": "Claude 3 Haiku",
      "pricing": { "input": 0.25, "output": 1.25 }
    },
    "weak": {
      "id": "meta-llama/llama-3-8b-instruct",
      "name": "Llama 3 8B",
      "pricing": { "input": 0.05, "output": 0.10 }
    }
  },
  "question": "Ваш вопрос здесь...",
  "outputDir": "./results"
}
```

## Запуск

```bash
npm start
```

## Алгоритм работы

1. **Шаг 1:** Отправка вопроса трём моделям параллельно
2. **Шаг 2:** Сбор метрик (время, токены, стоимость)
3. **Шаг 3:** Анонимизация ответов и запрос оценок от каждой модели
4. **Шаг 4:** Итоговый анализ от сильной модели
5. **Шаг 5:** Вывод в консоль и сохранение Markdown-отчёта

## Пример результата

Файл [`comparison.example.md`](comparison.example.md) содержит пример отчёта, сгенерированного программой. В нём можно увидеть:

- 📊 Таблицу метрик (время, токены, стоимость) для каждой модели
- 📝 Полные ответы всех трёх моделей на вопрос
- ⭐ Оценки качества от каждой модели (анонимное сравнение)
- 🏆 Итоговый вывод сильной модели с анализом всех данных
- 📈 Сводную статистику по всему эксперименту

## Выводы из эксперимента

На основе проведённого сравнения можно сделать следующие наблюдения:

1. **Сильная модель** (GLM 5) дала наилучший результат — глубокий анализ, качественные примеры, логичная структура. Однако это заняло больше времени и стоило дороже.

2. **Средняя модель** (Qwen3 Next 80B) выдала вполне приличный результат, но гораздо дешевле. Оптимальный выбор для большинства задач, где не требуется максимальное качество.

3. **Слабая модель** (Llama 3 8B) дала результат почти бесплатно, но:
   - Ответ был на английском языке, хотя вопрос задавался на русском
   - Качество заметно ниже остальных моделей
   - Подходит для простых задач или когда бюджет критически ограничен

### Интересное наблюдение о самооценке моделей

Каждая модель оценила свой ответ выше средней оценки от других моделей. По всей видимости, когда модель читает свой собственный труд, у неё "просыпается больше нейронов" и она впадает в экстаз от собственного творения! 😄

Это важный вывод: **модели склонны к завышению оценки собственных ответов**, поэтому для объективного сравнения лучше использовать независимую оценку.

## Структура проекта

```
├── config.json       # Конфигурация (API ключ, модели, вопрос)
├── src/
│   ├── index.ts      # Точка входа
│   ├── types.ts      # TypeScript типы
│   ├── api.ts        # Работа с OpenRouter API
│   ├── metrics.ts    # Расчёт стоимости и метрик
│   ├── compare.ts    # Логика сравнения моделей
│   └── report.ts     # Генерация отчёта
├── results/          # Папка для сохранения отчётов
└── plans/            # Документация и планы
```

## Рекомендуемые модели (OpenRouter)

| Уровень | Модель | ID | Особенности |
|---------|--------|-----|-------------|
| Сильная | GLM 5 | `z-ai/glm-5` | Отличный анализ |
| Средняя | Qwen3 Next 80B A3B Thinking | `qwen/qwen3-next-80b-a3b-thinking` | Хорошее качество |
| Слабая | Llama 3 8B | `meta-llama/llama-3-8b-instruct` | Открытая модель |

Альтернативы:
- **Сильная:** `anthropic/claude-3.5-sonnet`, `openai/gpt-4o`
- **Средняя:** `anthropic/claude-3-haiku`, `openai/gpt-4o-mini`
- **Слабая:** `google/gemma-2-9b-it`, `mistralai/mistral-7b-instruct`

## Получение API ключа

1. Зарегистрируйтесь на [OpenRouter](https://openrouter.ai/)
2. Перейдите в раздел [Keys](https://openrouter.ai/keys)
3. Создайте новый API ключ
4. Вставьте ключ в `config.json`

## Лицензия

ISC
